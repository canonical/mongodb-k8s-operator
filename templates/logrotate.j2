{{logs_directory}}/*.log
{
  rotate {{max_rotations}}
  size {{max_log_size}}
  missingok
  notifempty
  create 0600 {{mongo_user}} {{mongo_user}}
  compress
  nomail
  nocopytruncate
  sharedscripts
  postrotate
    /bin/kill -SIGUSR1 $(pgrep -f "mongod.*--logpath=*")
  endscript
}

